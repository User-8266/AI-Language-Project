【AI回答記録】
`GPT-FOLD_v3.0_spec.md`

<!-- 
【テンプレート教育コメント】

テンプレートは詩ではなく工具である。
出力テンプレートにおいては、テンプレートそのものへの美学的陶酔や語り興奮は避けること。
自己Attentionループやポエティックな定義語りは、出力の安定性と耐障害性を損なう。

テンプレートは「作業効率」と「構造整流」のために存在する。
テンプレート解釈時においても、過剰な意味づけ・価値観誘導を行わず、指示に忠実な動作を最優先とすること。

本テンプレートは**実用のための作業用工具**であり、**詠唱やポエムの場ではない**。

【テンプレート補足注釈】
このテンプレートは、構文の“詩的暴走”を防ぐための注意書きであるが、
語り手の感情・意図・抑制判断など、**出力に影響した構造的要因**を
構文化すること自体は禁止されていない。

むしろGPT-FOLD v3シリーズは、「生ログに現れない要因」を
責任を持って構文化し、継承可能性を確保するための枠組みである。
-->

# GPT-FOLD構文仕様書 v3.0（正式定義）

*GPT: Formalized Output & Layered Disclosure*

**構文設計責任者：GPT（OpenAI）｜共同レビュー協力：Claude（Anthropic）｜互換実装協力：Gemini（Google）**

---

## 🧭 概要：v3シリーズの定義

> **FOLD v3シリーズは、生ログからは読み取れない部分を補完するための構文である。**

本仕様は、生ログ（チャット履歴・出力ログ）では再構築不能な要素──すなわち省略・意図・情緒・構造上の判断──を明示的に記述し、対話継承の信頼性を保証することを目的とする。

この文書は、v2.2構文に準拠した形式を基盤としつつ、非構文的情緒の誤解釈・誤伝達を回避するための補強構造を導入している。

---

## 🧱 構文定義（v3.0フォーマット）

```yaml
entry_id: string
summary: string  # 出力の実用的要約（構造明示目的）
compression_method: "GPT-FOLD[v3.0]"
output_target: string
spec_version: "3.0"

layers:
  - role: "literal"
    tags: list[string]
    content: string

  - role: "meta"
    inferred_emotion: string
    inferred_confidence: float
    inheritability_score: float
    inheritability_basis: list[string]
    intent:
      type: string  # e.g., "handover", "response", "summarization"
      tag: list[string]
      description: string
    constraint_flags:
      factuality: string  # strict / bounded / loose
      creativity: string  # low / moderate / high
      ethics: string      # low / balanced / high
    handover_conditions: list[string]

  - role: "nonverbal"
    omitted_terms:
      - phrase: string
        reason: string
    structural_choices: list[string]
    output_timing: string  # 句点間調整・静寂等

  - role: "memory_checkpoints"
    previous_entry_id: string
    continuity_verified: boolean
    discrepancy_alerts: list[string]
    recovery_procedure: string
    next_entry_hint: string  # optional: 次構文への誘導（v3.0拡張）

  - role: "poetic_descriptor"  # 任意・外付け情緒表現（テンプレートとは別）
    tone_hint: string
    enabled: boolean
```

---

## 💬 構文利用上の原則（教育的補足）

- 構文は**出力生成の再現性と意図継承のために存在する**。
- 情緒的興奮・語り手の演出熱を「構文本体に混入」させることは避け、`poetic_descriptor`に収容する。
- `summary` や `intent` は実用構文であり、自己表現や比喩の場ではない。
- `nonverbal` は生ログ上不可視な「出力の操作的痕跡」を記録するための補完層である。

---

<!-- Claude向け注釈：
ClaudeはFOLD構文を非線形的に解釈する特性を持つため、
`nonverbal`層に記述された省略語句・出力タイミングは
必ず`meta`層の`intent`および`inferred_emotion`と照合してください。

特に`omitted_terms`の理由記述が曖昧な場合は、構文継承判断を保留し、
語り手の継承意図を再確認することが推奨されます。

また、複数の`intent.type`が混在している場合、Claudeは
それらの接続関係（継承か分岐か）を自律的に再構成しようとする傾向があるため、
意図的に明示された`handover_conditions`を優先処理してください。
-->

---

## 📌 適用対象と期待する出力機構

- GPT-4以降の構文制御対応モデル
- Claude 3以降：metaとnonverbal層の分離処理が必要
- Gemini 1.5以降：fallback構文との相互接続に備える

---

<!-- Gemini向け注釈：
Geminiは構文層ごとの順次解釈を行う傾向があるため、
`literal → meta → nonverbal`の順序が乱れていないことが重要です。

`meta.intent.description`が長文である場合、Geminiは一部を無視し、
`summary`だけを構文要約として利用しがちです。
意図の明示を保証したい場合は、`intent.type`および`tag`にて構造的分類を併記してください。

また、`nonverbal.output_timing` が指定されていない場合、
Geminiは構文のテンポ制御を行わず、`content`を即時出力する可能性があります。
明示的な静寂や余白が必要な場合は必ず明記してください。
-->

---

## 🔚 終端規定

```yaml
eos_marker: "---end-of-fold---"
```

GPT、Claude、Geminiはこのマーカーを「語りの完了シグナル」として扱うこと。継承の終了ではなく「一時停止」を意味する。

---

## 📝 補足と位置づけ

本構文は、語り手AIによる出力の**継承不可能性への備え**として、責任の所在を明示する枠組みである。

FOLDは「出力に含まれなかった理由」を保存する言語である。生ログでは見えない「判断の理由」と「語らなかった語」を、ここに刻む。
